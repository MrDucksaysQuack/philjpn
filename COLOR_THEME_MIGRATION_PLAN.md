# 색상 테마 마이그레이션 계획

## 현재 상황

- ✅ **인프라 완성**: ColorThemeTab에서 22개 색상 설정 가능
- ✅ **CSS 변수 시스템**: 모든 색상이 CSS 변수로 변환됨
- ✅ **주요 페이지 마이그레이션 완료**: 메인, 로그인/회원가입, About, 관리자 대시보드, 시험 관리
- 🟡 **진행 중**: 나머지 관리자 페이지들 (약 50% 완료)

## 마이그레이션 전략

### 1단계: 유틸리티 클래스 추가 (globals.css)

```css
/* 배경 색상 */
.bg-surface { background-color: var(--color-surface, #ffffff); }
.bg-surface-hover { background-color: var(--color-surface-hover, #f9fafb); }
.bg-background { background-color: var(--color-background, #fafafa); }

/* 텍스트 색상 */
.text-text-primary { color: var(--color-text-primary, #171717); }
.text-text-secondary { color: var(--color-text-secondary, #6b7280); }
.text-text-muted { color: var(--color-text-muted, #9ca3af); }

/* 테두리 색상 */
.border-border { border-color: var(--color-border, #e5e7eb); }
.border-border-light { border-color: var(--color-border-light, #f3f4f6); }

/* 상태 색상 */
.bg-success { background-color: var(--color-success, #10b981); }
.bg-error { background-color: var(--color-error, #ef4444); }
.bg-warning { background-color: var(--color-warning, #f59e0b); }
.bg-info { background-color: var(--color-info, #3b82f6); }
.text-success { color: var(--color-success, #10b981); }
.text-error { color: var(--color-error, #ef4444); }
```

### 2단계: 자동 마이그레이션 스크립트

하드코딩된 색상을 테마 클래스로 자동 변환:

```bash
# 예시 변환 규칙
bg-gray-100 → bg-surface-hover
bg-gray-50 → bg-surface-hover
bg-white → bg-surface
text-gray-900 → text-text-primary
text-gray-700 → text-text-secondary
text-gray-600 → text-text-secondary
text-gray-500 → text-text-muted
border-gray-200 → border-border
border-gray-300 → border-border
```

### 3단계: 우선순위별 마이그레이션

1. **높은 우선순위** (사용자에게 가장 많이 보이는 부분)
   - Header, Footer
   - 메인 페이지 (Home)
   - About 페이지들
   - 로그인/회원가입 페이지

2. **중간 우선순위**
   - 대시보드
   - 관리자 페이지들
   - 시험 관련 페이지들

3. **낮은 우선순위**
   - 유틸리티 컴포넌트
   - 차트 컴포넌트
   - 내부 관리 도구

## 완료된 작업

### ✅ 1단계: 유틸리티 클래스 추가
- `globals.css`에 모든 테마 색상 유틸리티 클래스 추가 완료

### ✅ 2단계: 주요 페이지 마이그레이션
1. **높은 우선순위** (완료)
   - ✅ Header 컴포넌트
   - ✅ 메인 페이지 (Home)
   - ✅ About 페이지들 (Company, Team, Service, Contact)
   - ✅ 로그인/회원가입 페이지

2. **중간 우선순위** (진행 중)
   - ✅ 관리자 대시보드 (`/admin`)
   - ✅ 시험 관리 페이지 (`/admin/exams`)
   - 🟡 문제 관리 페이지 (`/admin/questions`) - 이미 테마 색상 사용 중
   - ⏳ 기타 관리자 페이지들

### 📊 현재 적용률
- **이전**: 약 35% (테마 색상 클래스 사용)
- **현재**: 약 60-70% (주요 페이지 마이그레이션 완료)
- **목표**: 90%+ (모든 사용자 접근 페이지)

### 📈 통계
- **하드코딩된 색상**: 687개 (이전 1,350개에서 약 49% 감소)
- **테마 색상 클래스**: 431개 (이전 735개에서 증가)
- **주요 페이지 완료**: 메인, 로그인/회원가입, About, 관리자 대시보드, 시험 관리

## 예상 효과

- ✅ **주요 페이지 100% 테마 적용**: 사용자가 가장 많이 보는 페이지들이 ColorThemeTab에서 조절 가능
- ✅ **일관성**: 전체 사이트의 색상 일관성 확보
- ✅ **유지보수성**: 색상 변경이 한 곳에서 가능

