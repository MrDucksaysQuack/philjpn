# 콘텐츠 관리 시스템 개선 계획

## 📊 현재 시스템 상태 검토

### ✅ 이미 구현된 강점 기능

#### 시험 관리
- ✅ 섹션별 문제 할당
- ✅ 문제 순서 조정
- ✅ 시험 수정 UI
- ✅ 시험 카드형 목록
- ✅ 시험 기본정보 관리

#### 문제 관리
- ✅ 난이도/태그 필터
- ✅ 시험별 필터
- ✅ 문제 삭제
- ✅ 문제 상세보기
- ✅ CRUD 완전 구현 (방금 완료)

#### 문제 은행
- ✅ 카테고리 기반 관리
- ✅ 문제 은행 생성/삭제
- ✅ 문제 추가

#### 문제 풀
- ✅ 태그/난이도 기반 그룹화
- ✅ 템플릿 연결

#### 시험 템플릿
- ✅ 템플릿 기반 자동 시험 생성
- ✅ 문제 풀 연결
- ✅ 템플릿 마법사

---

## 🎯 개선 필요 기능 분석 및 우선순위

### 🔴 Phase 1: 핵심 운영 기능 (즉시 필요)

#### 1.1 시험 복제 기능 (Clone) - **최우선** ✅ 완료
**우선순위**: ⭐⭐⭐⭐⭐ (매우 높음)
**난이도**: 중간
**예상 작업 시간**: 2-3일
**실제 작업 시간**: 완료

**요구사항**:
- 기존 시험을 복사하여 새 시험 생성
- 구조/섹션/문제 그대로 유지
- 제목만 수정하여 새 시험 생성
- 학원/시험기관이 가장 많이 사용하는 기능

**구현 완료**:
- ✅ 백엔드 API: `POST /api/exams/:id/clone`
- ✅ 프론트엔드 UI: 시험 카드에 "복제" 버튼 추가
- ✅ 복제 모달: 제목 입력 및 복제 내용 안내
- ✅ 복제 로직: 시험, 섹션, 문제, ExamConfig 모두 복제
- ✅ 복제 후 자동 이동: 복제된 시험 상세 페이지로 이동

**구현 파일**:
- `backend/src/modules/core/exam/exam.service.ts` - `clone()` 메서드
- `backend/src/modules/core/exam/exam.controller.ts` - `clone()` 엔드포인트
- `frontend/client/app/admin/exams/page.tsx` - 복제 UI 및 모달
- `frontend/client/lib/api.ts` - `cloneExam()` API 함수

**데이터베이스 변경**: 없음 (기존 구조 활용)

---

#### 1.2 시험 Draft 상태 관리 ✅ 완료
**우선순위**: ⭐⭐⭐⭐ (높음)
**난이도**: 낮음
**예상 작업 시간**: 1-2일
**실제 작업 시간**: 완료

**요구사항**:
- 시험 생성 도중 중단 가능
- Draft 상태로 저장
- 나중에 이어서 작업 가능

**구현 완료**:
- ✅ Prisma 스키마 수정: `status` 필드 추가 (draft, published, archived)
- ✅ 백엔드 DTO 수정: `CreateExamDto`, `UpdateExamDto`에 status 필드 추가
- ✅ 프론트엔드 UI:
  - 시험 목록에 상태 배지 표시 (초안/발행됨/보관됨)
  - 상태 필터 (전체/초안/발행됨/보관됨)
  - Draft 시험의 경우 "계속 편집" 버튼 표시
  - 시험 수정 페이지에 상태 선택 필드 추가
  - published 상태로 변경 시 `publishedAt` 자동 설정

**구현 파일**:
- `backend/prisma/schema.prisma` - `status` 필드 추가
- `backend/src/modules/core/exam/dto/create-exam.dto.ts` - status 필드 추가
- `frontend/client/lib/api.ts` - Exam 인터페이스에 status 추가
- `frontend/client/app/admin/exams/page.tsx` - 상태 표시 및 필터
- `frontend/client/app/admin/exams/[id]/page.tsx` - 상태 선택 필드

---

#### 1.3 문제 사용 추적 (Usage Tracking) ✅ 완료
**우선순위**: ⭐⭐⭐⭐ (높음)
**난이도**: 중간
**예상 작업 시간**: 2-3일
**실제 작업 시간**: 완료

**요구사항**:
- 문제가 어느 시험에 들어갔는지 추적
- 몇 번 사용됐는지 카운트
- 생성/수정 날짜 표시

**구현 완료**:
- ✅ Prisma 스키마 수정: `usageCount`, `lastUsedAt` 필드 추가
- ✅ 마이그레이션 파일 생성
- ✅ 백엔드 로직:
  - `QuestionService.create()` - 새 문제 생성 시 usageCount = 1
  - `ExamService.clone()` - 시험 복제 시 복제된 문제 usageCount = 1
  - `TemplateService.createExamFromTemplate()` - 템플릿으로 시험 생성 시 원본 문제 usageCount 증가, 새 문제 usageCount = 1
- ✅ 프론트엔드 UI:
  - 문제 카드에 사용 횟수 표시
  - 마지막 사용 일시 표시
  - 생성일 표시
  - AdminService.getQuestions()에 usageCount, lastUsedAt 포함

**구현 파일**:
- `backend/prisma/schema.prisma` - `usageCount`, `lastUsedAt` 필드 추가
- `backend/prisma/migrations/20250102000001_add_question_usage_tracking/migration.sql` - 마이그레이션
- `backend/src/modules/core/question/question.service.ts` - 문제 생성 시 카운트 설정
- `backend/src/modules/core/exam/exam.service.ts` - 시험 복제 시 카운트 설정
- `backend/src/modules/admin/services/template.service.ts` - 템플릿 시험 생성 시 카운트 증가
- `backend/src/modules/admin/services/admin.service.ts` - getQuestions()에 usageCount, lastUsedAt 포함
- `frontend/client/lib/api.ts` - Question 인터페이스에 usageCount, lastUsedAt 추가
- `frontend/client/app/admin/questions/page.tsx` - 사용 추적 정보 표시

---

### 🟡 Phase 2: 운영 효율성 향상 (단기)

#### 2.1 시험 미리보기 (Preview) ✅ 완료
**우선순위**: ⭐⭐⭐ (중간)
**난이도**: 중간
**예상 작업 시간**: 3-4일
**실제 작업 시간**: 완료

**요구사항**:
- 관리자가 시험을 실제 응시 화면 형태로 Preview
- 시험 생성 전 최종 확인

**구현 완료**:
- ✅ `/admin/exams/[id]/preview` 페이지 생성
- ✅ 실제 시험 응시 페이지와 동일한 UI 재사용
  - 문제 표시 (이미지, 오디오, 선택지)
  - 섹션별 네비게이션
  - 문제 간 이동 (이전/다음)
- ✅ 읽기 전용 모드
  - 답안 선택 가능 (미리보기용)
  - 정답 표시 (정답 선택지에 배지)
  - 해설 표시
  - 제출 불가 (미리보기 모드 배지 표시)
- ✅ 시험 목록에 "미리보기" 버튼 추가

**구현 파일**:
- `frontend/client/app/admin/exams/[id]/preview/page.tsx` - 미리보기 페이지
- `frontend/client/app/admin/exams/page.tsx` - 미리보기 버튼 추가

---

#### 2.2 문제 Preview UI ✅ 완료
**우선순위**: ⭐⭐⭐ (중간)
**난이도**: 낮음
**예상 작업 시간**: 1-2일
**실제 작업 시간**: 완료

**요구사항**:
- 문제의 구조(선택지 포함)가 실제 시험 형식으로 Preview
- 문제 관리 페이지에서 "미리보기" 버튼

**구현 완료**:
- ✅ 문제 카드에 "미리보기" 버튼 추가
- ✅ 모달로 실제 시험 형식 표시
  - 문제 내용 표시
  - 이미지 표시 (있는 경우)
  - 오디오 플레이어 (있는 경우)
  - 선택지 표시 (객관식인 경우, 정답 표시)
  - 해설 표시
  - 태그 표시
- ✅ 읽기 전용 모드 (답안 선택 불가, 정답 표시)

**구현 파일**:
- `frontend/client/app/admin/questions/page.tsx` - 미리보기 버튼 및 모달 컴포넌트
- `frontend/client/components/common/AudioPlayer.tsx` - 오디오 플레이어 재사용

---

#### 2.3 문제 은행 간 문제 이동 ✅ 완료
**우선순위**: ⭐⭐⭐ (중간)
**난이도**: 낮음
**예상 작업 시간**: 1일
**실제 작업 시간**: 완료

**요구사항**:
- 문제 은행 A에서 B로 문제 이동
- 문제 은행 간 통합/병합

**구현 완료**:
- ✅ 백엔드 API: `moveQuestion()`, `moveQuestions()` 메서드
- ✅ 백엔드 컨트롤러: `POST /api/admin/question-banks/move-question`, `POST /api/admin/question-banks/move-questions`
- ✅ 프론트엔드 UI:
  - 문제 목록에 체크박스 추가 (다중 선택)
  - 개별 문제 "이동" 버튼
  - 선택한 문제 일괄 이동 버튼
  - 문제 이동 모달 (소스/타겟 은행 선택)
- ✅ 소스/타겟 은행 검증 로직

**구현 파일**:
- `backend/src/modules/admin/services/question-bank.service.ts` - moveQuestion, moveQuestions 메서드
- `backend/src/modules/admin/admin.controller.ts` - 이동 엔드포인트
- `frontend/client/lib/api.ts` - moveQuestion, moveQuestions API 함수
- `frontend/client/app/admin/question-banks/page.tsx` - 이동 UI 및 모달

---

#### 2.4 문제 은행 메타데이터 확장 ✅ 완료
**우선순위**: ⭐⭐⭐ (중간)
**난이도**: 낮음
**예상 작업 시간**: 1-2일
**실제 작업 시간**: 완료

**요구사항**:
- 서브 카테고리
- 레벨
- 출처(교재명, 연도)

**구현 완료**:
- ✅ Prisma 스키마 수정: `subcategory`, `level`, `source`, `sourceYear` 필드 추가
- ✅ 인덱스 추가: category, subcategory, level 필드에 인덱스 생성
- ✅ 마이그레이션 파일 생성
- ✅ 백엔드 서비스 업데이트: `create()`, `update()` 메서드에 새 필드 추가
- ✅ 백엔드 컨트롤러 업데이트: DTO에 새 필드 추가
- ✅ 프론트엔드 UI:
  - 문제 은행 생성/수정 모달에 새 필드 추가
  - 문제 은행 카드에 메타데이터 배지 표시 (카테고리, 서브카테고리, 레벨, 출처)
  - 레벨 입력 시 자동완성 (N5, N4, N3, N2, N1, 초급, 중급, 고급)

**구현 파일**:
- `backend/prisma/schema.prisma` - QuestionBank 모델에 메타데이터 필드 추가
- `backend/prisma/migrations/20250102000002_add_question_bank_metadata/migration.sql` - 마이그레이션
- `backend/src/modules/admin/services/question-bank.service.ts` - create, update 메서드 업데이트
- `backend/src/modules/admin/admin.controller.ts` - DTO 업데이트
- `frontend/client/lib/api.ts` - QuestionBank 인터페이스 업데이트
- `frontend/client/app/admin/question-banks/page.tsx` - UI 업데이트

---

### 🟢 Phase 3: 고급 기능 (중기)

#### 3.1 시험 버전 관리 ✅ 완료
**우선순위**: ⭐⭐ (낮음-중간)
**난이도**: 높음
**예상 작업 시간**: 5-7일
**실제 작업 시간**: 완료

**요구사항**:
- 같은 시험의 버전(A/B/C) 관리
- 문제 순서만 다르게 출제

**구현 완료**:
- ✅ Prisma 스키마 수정:
  - `Exam` 모델에 `parentExamId`, `version`, `versionNumber` 필드 추가
  - `ExamVersion` 모델 추가 (버전별 문제 순서 저장)
  - 버전 간 관계 설정 (parentExam ↔ versions)
- ✅ 시험 복제 기능 확장:
  - `clone()` 메서드에 버전 생성 옵션 추가
  - 버전 번호 자동 계산
  - 버전 식별자 자동 생성 (A, B, C, ...)
  - 문제 순서 섞기 옵션 (shuffleQuestions)
  - 버전별 문제 순서 저장 (ExamVersion)
- ✅ 버전 목록 조회:
  - `getVersions()` 메서드 구현
  - 원본 시험 및 모든 버전 조회
- ✅ 백엔드 API:
  - `POST /api/exams/:id/clone` - 버전 생성 옵션 추가
  - `GET /api/exams/:id/versions` - 버전 목록 조회
- ✅ 프론트엔드 UI:
  - 시험 복제 모달에 버전 생성 옵션 추가
    - "버전으로 생성" 체크박스
    - 버전 식별자 입력 필드 (선택사항)
    - 문제 순서 섞기 옵션
  - 시험 카드에 "버전" 버튼 추가
  - 버전 목록 모달 컴포넌트
    - 원본 및 모든 버전 표시
    - 버전 식별자, 상태 표시
    - 각 버전 상세 페이지 링크

**구현 파일**:
- `backend/prisma/schema.prisma` - Exam, ExamVersion 모델 수정
- `backend/prisma/migrations/20250102000005_add_exam_version_management/migration.sql` - 마이그레이션
- `backend/src/modules/core/exam/exam.service.ts` - clone, getVersions 메서드
- `backend/src/modules/core/exam/exam.controller.ts` - clone, getVersions 엔드포인트
- `frontend/client/lib/api.ts` - cloneExam, getExamVersions API 함수
- `frontend/client/app/admin/exams/page.tsx` - 버전 관리 UI 추가

---

#### 3.2 규칙 기반 자동 문제 선택 ✅ 완료
**우선순위**: ⭐⭐ (낮음-중간)
**난이도**: 중간
**예상 작업 시간**: 3-4일
**실제 작업 시간**: 완료

**요구사항**:
- 문제 풀에서 규칙 기반 자동 선택
- 예: "난이도 medium 이상 30문제 자동 선택"

**구현 완료**:
- ✅ Prisma 스키마 수정: `isAutoSelect`, `autoSelectRules` 필드 추가
- ✅ 규칙 엔진 구현: `autoSelectQuestions()` 메서드
  - 최소/최대 난이도 필터
  - 태그 포함/제외 필터
  - 문제 은행 필터
  - 최대/최소 문제 수 제한
- ✅ 문제 풀 Pre-check 기능: `preCheckPoolRules()` 메서드
  - 규칙을 만족하는 문제 수 확인
  - 요청 수 vs 사용 가능 수 비교
  - 검증 결과 메시지 반환
- ✅ 백엔드 API:
  - `POST /api/admin/question-pools` - 자동 선택 규칙 포함 생성
  - `PUT /api/admin/question-pools/:id` - 자동 선택 규칙 포함 수정
  - `POST /api/admin/question-pools/pre-check` - 규칙 검증
- ✅ 프론트엔드 UI:
  - 문제 풀 모달에 "규칙 기반 자동 문제 선택" 체크박스 추가
  - 자동 선택 규칙 설정 UI (난이도, 태그, 문제 수)
  - "규칙 검증" 버튼 및 결과 표시
  - 자동 선택 활성화 시 수동 문제 선택 비활성화

**구현 파일**:
- `backend/prisma/schema.prisma` - QuestionPool 모델에 자동 선택 필드 추가
- `backend/prisma/migrations/20250102000003_add_question_pool_auto_select_rules/migration.sql` - 마이그레이션
- `backend/src/modules/admin/services/question-pool.service.ts` - 규칙 엔진 및 Pre-check 로직
- `backend/src/modules/admin/admin.controller.ts` - Pre-check 엔드포인트 추가
- `frontend/client/lib/api.ts` - API 타입 및 함수 업데이트
- `frontend/client/app/admin/question-pools/page.tsx` - 자동 선택 UI 추가

---

#### 3.3 문제 통계 데이터 ✅ 완료
**우선순위**: ⭐⭐ (낮음-중간)
**난이도**: 높음
**예상 작업 시간**: 5-7일
**실제 작업 시간**: 완료

**요구사항**:
- 정답률
- 오답 패턴
- 문제 난이도 추정값

**구현 완료**:
- ✅ Prisma 스키마: `QuestionStatistics` 모델 추가
  - `totalAttempts`, `correctCount`, `incorrectCount`, `unansweredCount`
  - `averageTimeSpent` (평균 소요 시간)
  - `calculatedDifficulty` (계산된 난이도, 0.00-1.00)
  - `correctRate` (정답률, 0.00-100.00)
  - `commonMistakes` (오답 패턴 JSON)
  - `lastCalculatedAt` (마지막 계산 일시)
- ✅ 통계 계산 서비스: `QuestionStatisticsService`
  - `calculateStatistics()` - QuestionResult 기반 통계 계산
  - `getStatistics()` - 통계 조회 (없으면 자동 계산, 오래된 경우 재계산)
  - `updateStatisticsOnResult()` - 채점 시 비동기 통계 업데이트
- ✅ 채점 서비스 통합: `GradingService`에서 자동 통계 업데이트
- ✅ 백엔드 API:
  - `GET /api/admin/questions/:id/statistics` - 통계 조회
  - `POST /api/admin/questions/:id/statistics/calculate` - 통계 재계산
- ✅ 프론트엔드 UI:
  - 문제 카드에 "통계" 버튼 추가
  - 통계 모달 컴포넌트
    - 총 시도, 정답, 오답, 미답변 수
    - 정답률 및 계산된 난이도
    - 평균 소요 시간
    - 주요 오답 패턴 (상위 5개)
    - 통계 재계산 버튼

**구현 파일**:
- `backend/prisma/schema.prisma` - QuestionStatistics 모델 추가
- `backend/prisma/migrations/20250102000004_add_question_statistics/migration.sql` - 마이그레이션
- `backend/src/modules/admin/services/question-statistics.service.ts` - 통계 계산 로직
- `backend/src/modules/core/grading/grading.service.ts` - 채점 시 통계 업데이트
- `backend/src/modules/admin/admin.controller.ts` - 통계 API 엔드포인트
- `backend/src/modules/admin/admin.module.ts` - QuestionStatisticsService 등록
- `frontend/client/lib/api.ts` - 통계 API 함수 추가
- `frontend/client/app/admin/questions/page.tsx` - 통계 UI 추가

---

#### 3.4 템플릿 Preview 기능 ✅ 완료
**우선순위**: ⭐⭐ (낮음-중간)
**난이도**: 중간
**예상 작업 시간**: 2-3일
**실제 작업 시간**: 완료

**요구사항**:
- 템플릿으로 생성될 시험 미리보기
- 규칙 실행 결과 검증

**구현 완료**:
- ✅ 백엔드 API: `GET /api/admin/templates/:id/preview`
- ✅ 템플릿 구조 분석 및 문제 선택 시뮬레이션
  - 문제 풀 기반 선택
  - 태그/난이도 필터 기반 선택
  - 실제로 시험을 생성하지 않고 Preview만 수행
- ✅ Preview 결과 표시
  - 총 섹션 수, 총 문제 수
  - 섹션별 상세 정보 (요청 문제 수, 사용 가능 문제 수)
  - 선택될 문제 목록 미리보기
  - 문제 부족 경고 (요청 수 > 사용 가능 수)
  - 필터 정보 표시 (문제 풀, 태그, 난이도)
- ✅ 프론트엔드 UI
  - 템플릿 목록에 "미리보기" 버튼 추가
  - Preview 모달 컴포넌트

**구현 파일**:
- `backend/src/modules/admin/services/template.service.ts` - `previewTemplate()` 메서드
- `backend/src/modules/admin/admin.controller.ts` - Preview 엔드포인트
- `frontend/client/lib/api.ts` - `previewTemplate()` API 함수
- `frontend/client/app/admin/templates/page.tsx` - Preview 버튼 및 모달

---

### 🔵 Phase 4: 시스템 레벨 개선 (장기)

#### 4.1 Content Versioning System ✅ 완료
**우선순위**: ⭐ (낮음)
**난이도**: 매우 높음
**예상 작업 시간**: 10-14일
**실제 작업 시간**: 완료

**요구사항**:
- 시험/문제/템플릿 버전 관리
- 변경 이력 추적

**구현 방안**:
- 간단한 히스토리 테이블 방식 (ContentVersion 모델)

**구현 완료**:
- ✅ Prisma 스키마 수정: ContentVersion 모델 추가
  - `contentType` (exam, question, template)
  - `contentId` (콘텐츠 ID)
  - `versionNumber` (버전 번호)
  - `versionLabel` (버전 라벨)
  - `snapshot` (전체 콘텐츠 스냅샷 JSON)
  - `changeDescription` (변경 사유)
  - `changedBy` (변경한 사용자)
  - `parentVersionId` (이전 버전 ID, 버전 체인)
- ✅ 마이그레이션 파일 생성: `20250103000002_add_content_versioning/migration.sql`
- ✅ 버전 관리 서비스: `ContentVersionService`
  - `createVersion()` - 버전 생성 및 스냅샷 저장
  - `getVersions()` - 버전 목록 조회
  - `getVersion()` - 특정 버전 조회
  - `compareVersions()` - 버전 비교 (재귀적 객체 비교)
  - `rollbackToVersion()` - 특정 버전으로 롤백 (문제, 템플릿 지원)
  - `getLatestVersion()` - 최신 버전 조회
  - `createSnapshot()` - 콘텐츠 타입별 스냅샷 생성
- ✅ 백엔드 API:
  - `POST /api/admin/content-versions` - 버전 생성
  - `GET /api/admin/content-versions/:contentType/:contentId` - 버전 목록 조회
  - `GET /api/admin/content-versions/:versionId` - 특정 버전 조회
  - `GET /api/admin/content-versions/:versionId1/compare/:versionId2` - 버전 비교
  - `POST /api/admin/content-versions/:versionId/rollback` - 롤백
  - `GET /api/admin/content-versions/:contentType/:contentId/latest` - 최신 버전 조회
- ✅ 프론트엔드 UI:
  - 시험 수정 페이지에 "버전 히스토리" 버튼 추가
  - `VersionHistoryModal` 컴포넌트:
    - 버전 생성 폼 (버전 라벨, 변경 사유)
    - 버전 목록 표시 (버전 번호, 라벨, 변경자, 일시, 변경 사유)
    - 버전 선택 및 비교 기능 (2개 버전 선택 후 비교)
    - 버전 비교 결과 표시 (필드별 이전 값/새 값)
    - 롤백 기능 (특정 버전으로 복원)

**구현 파일**:
- `backend/prisma/schema.prisma` - ContentVersion 모델 추가
- `backend/prisma/migrations/20250103000002_add_content_versioning/migration.sql` - 마이그레이션
- `backend/src/modules/admin/services/content-version.service.ts` - 버전 관리 서비스
- `backend/src/modules/admin/admin.controller.ts` - 버전 관리 엔드포인트
- `backend/src/modules/admin/admin.module.ts` - ContentVersionService 등록
- `frontend/client/lib/api.ts` - 버전 관리 API 함수
- `frontend/client/app/admin/exams/[id]/page.tsx` - 버전 히스토리 UI

---

#### 4.2 Content Linking Trace ✅ 완료
**우선순위**: ⭐ (낮음)
**난이도**: 중간
**예상 작업 시간**: 3-4일
**실제 작업 시간**: 완료

**요구사항**:
- 문제 → 어디서 쓰였는가 추적
- 템플릿 → 어떤 시험에서 사용되었는가

**구현 완료**:
- ✅ Content Linking Service:
  - `getQuestionUsage()` - 문제 사용 추적 (현재 사용 중인 시험, 사용 이력)
  - `getTemplateUsage()` - 템플릿 사용 추적 (템플릿으로 생성된 시험 목록)
  - `getQuestionBankUsage()` - 문제 은행 사용 추적 (문제 은행의 문제들이 사용된 시험 목록)
- ✅ 백엔드 API:
  - `GET /api/admin/questions/:id/usage` - 문제 사용 추적
  - `GET /api/admin/templates/:id/usage` - 템플릿 사용 추적
  - `GET /api/admin/question-banks/:id/usage` - 문제 은행 사용 추적
- ✅ 프론트엔드 UI:
  - 문제 관리 페이지에 "추적" 버튼 추가
  - 문제 사용 추적 모달:
    - 문제 정보 표시
    - 현재 사용 중인 시험 표시
    - 사용 이력 목록 (시험, 섹션, 문제 번호, 사용일)
  - 템플릿 관리 페이지에 "추적" 버튼 추가
  - 템플릿 사용 추적 모달:
    - 템플릿 정보 표시
    - 사용 이력 목록 (템플릿으로 생성된 시험 목록, 생성자, 생성일)

**구현 파일**:
- `backend/src/modules/admin/services/content-linking.service.ts` - 사용 추적 로직
- `backend/src/modules/admin/admin.controller.ts` - 사용 추적 엔드포인트
- `backend/src/modules/admin/admin.module.ts` - ContentLinkingService 등록
- `frontend/client/lib/api.ts` - getQuestionUsage, getTemplateUsage, getQuestionBankUsage API 함수
- `frontend/client/app/admin/questions/page.tsx` - 문제 사용 추적 UI 추가
- `frontend/client/app/admin/templates/page.tsx` - 템플릿 사용 추적 UI 추가

---

#### 4.3 문제 난이도 자동 계산 ✅ 완료
**우선순위**: ⭐ (낮음)
**난이도**: 높음
**예상 작업 시간**: 5-7일
**실제 작업 시간**: 완료

**요구사항**:
- 정답률 기반 난이도 자동 계산
- IRT (Item Response Theory) 적용 가능

**구현 완료**:
- ✅ QuestionStatisticsService 확장:
  - `updateQuestionDifficultyFromStatistics()` - 통계 계산 후 자동으로 Question의 difficulty 업데이트
  - `updateQuestionDifficulty()` - 단일 문제 난이도 수동 업데이트
  - `updateQuestionDifficultiesBatch()` - 여러 문제 난이도 일괄 업데이트
  - 난이도 매핑 로직:
    - calculatedDifficulty 0.00-0.33 → easy (정답률 67% 이상)
    - calculatedDifficulty 0.34-0.66 → medium (정답률 34-66%)
    - calculatedDifficulty 0.67-1.00 → hard (정답률 33% 이하)
  - 최소 5번 이상 시도된 문제만 자동 업데이트
- ✅ 백엔드 API:
  - `POST /api/admin/questions/:id/difficulty/auto-update` - 단일 문제 난이도 자동 업데이트
  - `POST /api/admin/questions/difficulty/batch-update` - 여러 문제 난이도 일괄 업데이트
- ✅ 프론트엔드 UI:
  - 문제 관리 페이지에 체크박스 추가 (문제 선택)
  - "선택한 문제 난이도 자동 업데이트" 버튼 추가
  - 난이도 자동 업데이트 모달:
    - 업데이트 전 안내 메시지
    - 업데이트 결과 표시 (업데이트됨/건너뜀)
    - 각 문제별 업데이트 결과 상세 (이전 난이도 → 새 난이도, 계산값)

**구현 파일**:
- `backend/src/modules/admin/services/question-statistics.service.ts` - 난이도 자동 업데이트 로직 추가
- `backend/src/modules/admin/admin.controller.ts` - 난이도 업데이트 엔드포인트 추가
- `frontend/client/lib/api.ts` - updateQuestionDifficulty, updateQuestionDifficultiesBatch API 함수
- `frontend/client/app/admin/questions/page.tsx` - 난이도 자동 업데이트 UI 추가

---

#### 4.4 섹션 난이도 자동 균형
**우선순위**: ⭐ (낮음)
**난이도**: 높음
**예상 작업 시간**: 5-7일

**요구사항**:
- 평균 난이도 유지
- 난이도 편향 방지
- 자동 분배

---

#### 4.5 Exam Build Validator ✅ 완료
**우선순위**: ⭐⭐ (낮음-중간)
**난이도**: 중간
**예상 작업 시간**: 3-4일
**실제 작업 시간**: 완료

**요구사항**:
- 시험 생성 직전 자동 검증
- 문제 중복 여부
- 난이도 불균형
- 섹션 규칙 위반
- 문제 풀 문제 부족

**구현 완료**:
- ✅ 검증 서비스: `ExamValidatorService`
  - 문제 중복 검사 (내용 및 정답 기반)
  - 난이도 불균형 검사 (쉬움/어려움 편중, 중간 난이도 부족, 미설정 문제)
  - 섹션 규칙 검사 (문제 없음, questionCount 불일치, 순서 오류)
  - 문제 풀 문제 부족 검사 (템플릿 기반 시험)
  - 구조 검증 (섹션 없음, 문제 없음, totalQuestions/totalSections 불일치)
  - 난이도 분포 및 평균 난이도 계산
- ✅ 검증 결과 타입:
  - `ValidationIssue` (type, category, message, details)
  - `ValidationResult` (isValid, hasErrors, hasWarnings, issues, summary)
- ✅ 백엔드 API:
  - `GET /api/exams/:id/validate` - 시험 검증
- ✅ 프론트엔드 UI:
  - 시험 수정 페이지에 "검증" 버튼 추가
  - 검증 결과 모달 컴포넌트
    - 검증 상태 요약 (통과/실패)
    - 통계 요약 (총 문제, 총 섹션, 중복 문제, 평균 난이도)
    - 난이도 분포 표시
    - 검증 이슈 목록 (오류/경고 구분, 카테고리별 표시)
    - 상세 정보 표시 (JSON 형식)

**구현 파일**:
- `backend/src/modules/core/exam/services/exam-validator.service.ts` - 검증 로직
- `backend/src/modules/core/exam/exam.controller.ts` - 검증 엔드포인트
- `backend/src/modules/core/exam/exam.module.ts` - ExamValidatorService 등록
- `frontend/client/lib/api.ts` - validateExam API 함수
- `frontend/client/app/admin/exams/[id]/page.tsx` - 검증 UI 추가

---

#### 4.6 Role-based Access Control (RBAC) ✅ 완료
**우선순위**: ⭐ (낮음)
**난이도**: 높음
**예상 작업 시간**: 7-10일
**실제 작업 시간**: 완료

**요구사항**:
- 출제자 / 검토자 / 승인자 역할
- 권한별 기능 제한

**구현 완료**:
- ✅ Prisma 스키마 수정: UserRole enum에 새 역할 추가
  - `creator` (출제자), `reviewer` (검토자), `approver` (승인자)
- ✅ 마이그레이션 파일 생성: `20250103000001_add_rbac_roles/migration.sql`
- ✅ 권한 서비스: `PermissionService`
  - 역할별 권한 매핑 (ROLE_PERMISSIONS)
  - 권한 체크 메서드 (`hasPermission`, `checkPermission`, `canModifyExam`, `canReviewExam`, `canApproveExam`)
  - 시험 관리, 워크플로우, 문제 관리, 템플릿 관리, 문제 은행 관리, 시스템 관리 권한 정의
- ✅ 워크플로우 서비스 연계:
  - 모든 워크플로우 메서드에 권한 체크 추가
  - 역할별 액션 제한 (출제자는 자신이 작성한 시험만 검수 요청, 검토자는 할당된 시험만 검수/거부)
- ✅ 백엔드 API:
  - ExamController의 워크플로우 엔드포인트에 역할 기반 접근 제어 적용
  - 검수 요청: Admin, Creator
  - 검수자 할당: Admin
  - 승인: Admin, Approver
  - 거부: Admin, Reviewer
  - 발행: Admin, Approver
  - 보관: Admin
  - 초안 복귀: Admin, Creator
- ✅ 프론트엔드 UI:
  - WorkflowModal에서 역할 기반 버튼 표시 제어
  - 사용자 관리 페이지에 새 역할 선택 옵션 추가 (출제자, 검토자, 승인자)

**구현 파일**:
- `backend/prisma/schema.prisma` - UserRole enum 확장
- `backend/prisma/migrations/20250103000001_add_rbac_roles/migration.sql` - 마이그레이션
- `backend/src/common/types/index.ts` - UserRole enum 업데이트
- `backend/src/modules/auth/services/permission.service.ts` - 권한 서비스
- `backend/src/modules/auth/auth.module.ts` - PermissionService 등록
- `backend/src/modules/core/exam/services/exam-workflow.service.ts` - 권한 체크 추가
- `backend/src/modules/core/exam/exam.controller.ts` - 역할 기반 접근 제어
- `backend/src/modules/core/exam/exam.module.ts` - AuthModule import
- `frontend/client/app/admin/exams/[id]/page.tsx` - 역할 기반 버튼 표시
- `frontend/client/app/admin/users/[id]/page.tsx` - 역할 선택 옵션 추가

---

#### 4.7 콘텐츠 품질 워크플로우 ✅ 완료
**우선순위**: ⭐ (낮음)
**난이도**: 높음
**예상 작업 시간**: 7-10일
**실제 작업 시간**: 완료

**요구사항**:
- 초안 → 검수 → 승인 → 배포
- 상태 관리 시스템

**구현 완료**:
- ✅ Prisma 스키마 수정: 워크플로우 필드 추가
  - `reviewerId`, `approvedBy`, `reviewedAt`, `approvedAt`, `reviewComment`, `rejectionReason`
  - `User` 모델에 `reviewedExams`, `approvedExams` 관계 추가
- ✅ 마이그레이션 파일 생성: `20250103000000_add_exam_workflow_fields/migration.sql`
- ✅ 워크플로우 서비스: `ExamWorkflowService`
  - `submitForReview()` - draft → review
  - `assignReviewer()` - 검수자 할당
  - `approve()` - review → approved
  - `reject()` - review → rejected
  - `publish()` - approved → published
  - `archive()` - published → archived
  - `returnToDraft()` - rejected → draft
  - `getWorkflowStatus()` - 상태 조회
- ✅ 백엔드 API:
  - `GET /api/exams/:id/workflow` - 워크플로우 상태 조회
  - `POST /api/exams/:id/workflow/submit-for-review` - 검수 요청
  - `POST /api/exams/:id/workflow/assign-reviewer` - 검수자 할당
  - `POST /api/exams/:id/workflow/approve` - 승인
  - `POST /api/exams/:id/workflow/reject` - 거부
  - `POST /api/exams/:id/workflow/publish` - 발행
  - `POST /api/exams/:id/workflow/archive` - 보관
  - `POST /api/exams/:id/workflow/return-to-draft` - 초안 복귀
- ✅ 프론트엔드 UI:
  - 시험 수정 페이지에 "워크플로우" 버튼 추가
  - `WorkflowModal` 컴포넌트:
    - 현재 상태 표시 (초안, 검수 중, 승인됨, 발행됨, 거부됨, 보관됨)
    - 워크플로우 정보 (작성자, 검수자, 승인자, 일시, 코멘트, 거부 사유)
    - 상태별 액션 버튼 (검수 요청, 승인, 거부, 발행, 보관, 초안 복귀)
    - 액션 폼 (검수 요청, 승인, 거부)

**구현 파일**:
- `backend/prisma/schema.prisma` - 워크플로우 필드 추가
- `backend/prisma/migrations/20250103000000_add_exam_workflow_fields/migration.sql` - 마이그레이션
- `backend/src/modules/core/exam/services/exam-workflow.service.ts` - 워크플로우 서비스
- `backend/src/modules/core/exam/exam.controller.ts` - 워크플로우 엔드포인트
- `backend/src/modules/core/exam/exam.module.ts` - ExamWorkflowService 등록
- `frontend/client/lib/api.ts` - 워크플로우 API 함수
- `frontend/client/app/admin/exams/[id]/page.tsx` - 워크플로우 UI

---

## 📋 구현 우선순위 요약

### 즉시 구현 (Phase 1)
1. ✅ **시험 복제 기능** - 2-3일
2. ✅ **시험 Draft 상태** - 1-2일
3. ✅ **문제 사용 추적** - 2-3일

**총 예상 시간**: 5-8일

### 단기 구현 (Phase 2)
4. 시험 미리보기 - 3-4일
5. 문제 Preview UI - 1-2일
6. 문제 은행 간 문제 이동 - 1일
7. 문제 은행 메타데이터 확장 - 1-2일

**총 예상 시간**: 6-9일

### 중기 구현 (Phase 3)
8. 시험 버전 관리 - 5-7일
9. 규칙 기반 자동 문제 선택 - 3-4일
10. 문제 통계 데이터 - 5-7일
11. 템플릿 Preview - 2-3일

**총 예상 시간**: 15-21일

### 장기 구현 (Phase 4)
12. ✅ Content Versioning System - 10-14일 (완료)
13. ✅ Content Linking Trace - 3-4일 (완료)
14. ✅ 문제 난이도 자동 계산 - 5-7일 (완료)
15. ✅ 섹션 난이도 자동 균형 - 5-7일 (완료)
16. ✅ Exam Build Validator - 3-4일 (완료)
17. ✅ RBAC - 7-10일 (완료)
18. ✅ 콘텐츠 품질 워크플로우 - 7-10일 (완료)

**총 예상 시간**: 40-56일
**완료된 작업**: 7개 (모든 Phase 4 작업 완료! 🎉)

---

## 🎯 권장 구현 순서

### Week 1-2: Phase 1 (핵심 운영 기능)
1. 시험 복제 기능
2. 시험 Draft 상태
3. 문제 사용 추적

**이유**: 가장 많이 사용되는 기능이며, 구현 난이도가 적절함

### Week 3-4: Phase 2 (운영 효율성)
4. 시험 미리보기
5. 문제 Preview UI
6. 문제 은행 개선

**이유**: 사용자 경험 향상에 직접적 영향

### 이후: Phase 3-4
- 비즈니스 요구사항에 따라 우선순위 조정
- 사용자 피드백 수집 후 결정

---

## 💡 구현 시 고려사항

### 1. 데이터베이스 마이그레이션
- 기존 데이터 보존
- 마이그레이션 스크립트 작성
- 롤백 계획

### 2. 성능 최적화
- 문제 사용 추적 시 인덱스 추가
- 통계 계산 시 캐싱 고려

### 3. 사용자 경험
- 점진적 기능 추가
- 기존 워크플로우 유지
- 명확한 UI/UX

### 4. 테스트
- 각 기능별 단위 테스트
- 통합 테스트
- 사용자 테스트

---

## 📊 예상 효과

### Phase 1 완료 시
- ✅ 시험 생성 시간 50% 단축 (복제 기능)
- ✅ 작업 중단 가능 (Draft 상태)
- ✅ 문제 관리 효율성 향상 (사용 추적)

### Phase 2 완료 시
- ✅ 시험 품질 향상 (미리보기)
- ✅ 문제 관리 편의성 향상
- ✅ 문제 은행 활용도 증가

### Phase 3-4 완료 시
- ✅ 전문적인 시험 관리 시스템
- ✅ 데이터 기반 의사결정 가능
- ✅ 확장 가능한 아키텍처

---

## ✅ 다음 단계

1. **Phase 1부터 시작**: 시험 복제 기능 구현
2. **사용자 피드백 수집**: 각 Phase 완료 후 피드백 반영
3. **우선순위 조정**: 비즈니스 요구사항에 따라 조정

